#!/bin/bash

if [ ! -d ".venv" ]; then
    echo "Virtual environment not found. Initializing..."
    ./jack-in.sh
fi

source .venv/bin/activate

if [ "$1" == "--setup" ]; then
    python src/setup_wizard.py
else
    python src/main.py
fi
